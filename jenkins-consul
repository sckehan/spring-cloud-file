pipeline {
   agent {label 'linux-205'}
   stages {
      
      stage('Pull Code') {
         steps {
            git 'https://github.com/sckehan/spring-cloud-examples.git'
            sh 'ls'
         }

      }
     
      stage('Build Code') {
         steps {
            sh 'cd spring-cloud-consul/spring-cloud-consul-producer && /usr/apache-maven-3.6.3/bin/mvn package'
            sh 'ls target/'
         }

      }

   }
}
