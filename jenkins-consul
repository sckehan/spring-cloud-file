pipeline {
   agent {label 'linux'}
   stages {
      
      stage('Pull Code') {
         steps {
            git 'https://github.com/ityouknow/spring-cloud-examples'
            sh 'ls'
         }

      }

   }
}
